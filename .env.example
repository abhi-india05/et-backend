# Runtime mode
ENVIRONMENT=production

# Core services
MONGODB_URI=mongodb+srv://<user>:<password>@<cluster>/<db>?retryWrites=true&w=majority
GEMINI_API_KEY=your-gemini-key

# Auth hardening
AUTH_SECRET_KEY=replace-with-a-long-random-secret-at-least-32-characters
AUTH_COOKIE_SECURE=true
AUTH_COOKIE_SAMESITE=none
AUTH_REFRESH_COOKIE_PATH=/

# CORS for frontend deployment(s)
CORS_ORIGINS=https://your-frontend-project.vercel.app
# Optional: include preview deployments with regex matching
# CORS_ORIGIN_REGEX=^https://.*\.vercel\.app$

# SMTP for deployed environments (Gmail app password recommended)
MAIL_USERNAME=your_email@gmail.com
MAIL_PASSWORD=your_gmail_app_password
MAIL_FROM=your_email@gmail.com
MAIL_FROM_NAME=RevOps AI
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_TLS=true
MAIL_SSL=false
# Optional: if you use implicit SSL on port 465, set MAIL_PORT=465, MAIL_SSL=true, MAIL_TLS=false
